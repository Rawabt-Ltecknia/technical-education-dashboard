import {aliases, mdi} from 'vuetify/iconsets/mdi'
import 'remixicon/fonts/remixicon.css';

export const icons = {
    defaultSet: 'mdi',
    aliases,
    sets: {
        // fa,
        // mdiSvg,
        mdi
    },
}
